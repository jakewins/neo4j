'use strict'

### A service that manages a common view of the graph for the entire app ###

angular.module('app.services.jmx', [])

.factory('jmxService', [
  '$http'
  '$rootScope'
  ($http, $rootScope)->
    class JmxService
    
      constructor : () ->
    
      
      
    new JmxService
  
])
